- pacman contains tools such as `makepkg` and `vercmp`. Other userful tools such as `pactree` and `checkupdates` are found in `pacman-contrib` 

    to see full list 
    # pacman -Ql pacman pacman contrib | grep -E 'bin/.+'

