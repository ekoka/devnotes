See: https://github.com/unbit/uwsgi/issues/1172

When the underlying uwsgi app fais to start, uwsgi still runs, instead of just failing. To force uwsgi to fail when no app is present add the `--need-app` flag.
