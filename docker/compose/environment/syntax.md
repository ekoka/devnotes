##### Ref: https://docs.docker.com/compose/environment-variables/env-file/

- referencing a variable

    # without string
    config=$VAR
    config=${VAR}

    # in string
    config="$VAR"
    config="${VAR}"
